# Fonts
title = ('Arial', 16)
buttons = ('Arial', 14)
information = ('Arial', 12)

# Paths
project_path_string = ''

# App dimensions
width = 600
height = 550

# Educational prompts
init_prompt = 'Running this command converts a folder in to your repository. All files within the folder will be part of your project.'
clone_prompt = 'This will retrive a github repo and install it locally on your machine at the specified path. The above code is what git displays while retriving the repo. If the repo fails, there will be an error message. The follow line of code was ran.'
name_prompt = 'The line below changes the name attached to your commits. The name assoicated with this command is how you are represented when you push your commits to a repo.'
email_prompt = 'The line below changes the email attached to your commits. The email assoicated with this command is how you are represented when you push your commits to a repo.'
status_prompt = 'Look at the terminal for your output. This command give you a snapshot of your project, which files are ready for the commit and which ones still need to be added or worked on.'
add_file_prompt = 'Files will be added to the staging area. These files are ready to be commited and pushed to the repo'
remove_file_prompt = 'Files will be removed from the staging are and will not be part of the commit and push to repo'

# Global Variables
name = ''
email = ''
